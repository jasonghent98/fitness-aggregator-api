spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fitdata
    username: your_db_user
    password: your_db_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: FitData