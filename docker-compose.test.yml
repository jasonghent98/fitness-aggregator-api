services:
  postgres:
    container_name: ${TEST_DB_CONTAINER_NAME}
    ports:
      - "${TEST_DB_PORT}:5432"
    environment:
      POSTGRES_DB: ${TEST_DB}
      POSTGRES_USER: ${TEST_DB_USER}
      POSTGRES_PASSWORD: ${TEST_DB_PASSWORD}